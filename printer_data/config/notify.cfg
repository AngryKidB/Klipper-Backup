[gcode_macro NOTIFY_FILAMENT_CHANGE]
gcode:
   {% set MSG = params.MSG|default("FILAMENT CHANGE REQUIRED")|string %}
    {action_call_remote_method("notify", name="notify_filament_change", message=MSG)}